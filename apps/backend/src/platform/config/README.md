# Platform Config Boundary

This directory is reserved for future backend platform configuration.

Stage 2B may document environment variable names, but it does not create runtime configuration loading or secret handling.

Candidate environment variables:

- `DATABASE_URL`
- `REDIS_URL`
- `SESSION_COOKIE_NAME`
- `NODE_ENV`
- `PORT`
